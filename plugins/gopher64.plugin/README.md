This plugin installs the Flatpak `io.github.gopher64.gopher64` (Gopher64) from Flathub.

Placeholder icon: `icon.svg` is provided; replace with a proper `icon.png` if you prefer.

Behavior:
- Uses the built-in Flatpak install/uninstall UI (no local scripts).
- Install runs via: `flatpak install --user -y flathub io.github.gopher64.gopher64`
